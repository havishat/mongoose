<!DOCTYPE html>
<html lang="en">
        <head>
          <meta charset="UTF-8">
          <title>Quotes</title>
        </head>
    <body>
        <h1>Here are the awesome quotes!</h1>
        <div id = "updateArea">
                <% for (var x in quotes) { %> 
                <h3> "<%= quotes[x].quote %>"</h3> 
                <h3> -<%= quotes[x].name %> at <%= quotes[x].createdAt %> </h3> 
                <hr> <% } %> 
        </div>
    </body>
</html>